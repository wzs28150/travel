# 2026-07-31 工作记录

## 本地启动项目（docker compose up -d）
- 三个容器全部跑通：travel_mysql(healthy,3306) / travel_backend(8089) / travel_frontend(8090→80)。
- 验证：http://localhost:8090/ → 200；/api/health → 200 {"code":0,"message":"ok"}。
- 踩坑：
  1. Docker Hub 不可达 → 须先 `docker pull docker.m.daocloud.io/library/mysql:8.0 && docker tag ... mysql:8.0`（node/nginx 昨天已拉好）。
  2. mysql 首次初始化约 2 分钟，第一次 `docker compose up -d` 因 `depends_on: mysql: service_healthy` 在 mysql 未就绪时失败退出，backend/frontend 卡在 Created。等 mysql healthy 后重跑 `docker compose up -d` 即全起。
- 顺带：compose 报 `version` 字段 obsolete 警告（无碍）。

## 注册后播种示例清单 + "示例"标签（功能完成）
- 需求：新用户注册后清单里有 1~2 条示例数据，卡片上打"示例"标签。
- 改动：
  1. `server/init.sql` 的 travels 表加 `is_sample TINYINT(1) DEFAULT 0`；运行中的 mysql 容器已 `ALTER TABLE` 加上（无需重建库）。
  2. `server/src/routes/travels.js`：`toTravel()` 返回 `isSample`；新增 `SAMPLE_TRAVELS`（京都赏樱、杭州西湖两条）+ `seedSampleTravels(userId)` 导出。
  3. `server/src/routes/auth.js`：register 成功后 `await seedSampleTravels(userId)`（try/catch 包裹，播种失败不影响注册）。
  4. `front-end/src/views/Lists.vue`：卡片封面左上角加 `v-if="t.isSample"` 的"示例"标签（右上角保留状态标签）。
- 验证：注册测试用户 → /api/travels 返回 2 条且 `isSample:true`（行程/行李/待办齐全），前端 chunk 含"示例"文案。测试用户已清理。
- 重建：`docker compose up -d --build`（mysql 不受影响，数据保留）。

## 示例相册测试数据（功能完成）
- 需求：示例清单的相册也要有内容。
- 改动：`server/src/routes/travels.js` 的 `SAMPLE_TRAVELS` 每条加 `photos:[{place,tag,c1,c2}]`（京都/杭州各 3 张），`svgPhoto()` 把渐变占位图转成内联 `data:image/svg+xml;charset=utf-8,...`（离线可用、国内服务器不裂图），`seedSampleTravels` 写入 `content.photos`。
- 前端无需改：`TravelDetail.vue` 相册网格读 `travel.photos[{url,place,tag}]` 已支持。
- 验证：注册测试用户 → 两条示例各 3 张相册（含 place/tag + data URI）。测试用户已清理。
- 重建：`docker compose up -d --build backend`。

## git 同步 + 重建镜像（本次）
- 坑：沙箱 git 全局代理又复活指向 `127.0.0.1:11888`，导致 `git fetch` 连不上 github（port 443 via 127.0.0.1）。修复：`git config --global --unset http.proxy && git config --global --unset https.proxy`。
- 本地落后远程 3 个 commit（c4e4835/8555de2/00d8d0c 均为 fix：sharp 输出重名、t-icon onIconClick、移除清单筛选/相册按钮层级/批量上传）。
- 我们之前做的示例功能改动一直没 commit（Lists.vue/auth.js/travels.js/init.sql）。
- 流程：`git commit`（示例功能，127 行）→ `git pull --rebase origin main`（Lists.vue 自动合并无冲突，示例标签保留）→ `git push origin main`（43a81c6）。
- 重建：`docker compose up -d --build`，镜像含远程 3 fix + 示例功能。验证：注册用户→2 条示例各 3 张相册，前端含"示例"文案。测试用户已清理。

## 示例相册改用真实照片（本次）
- 需求：示例相册不要 SVG 色块，要真实照片。
- 实现：`server/src/routes/travels.js` 移除 `svgPhoto()`；photo 改为 LoremFlickr 真实 Flickr 照片直链 `https://loremflickr.com/800/600/{关键词}?lock=N`（按景点关键词对应 + lock 固定每张）。已 commit+push (`061ec64`)，重建 backend。
- 验证：注册用户→示例相册返回 https 真实图 URL（非 data: SVG）。测试用户已清理。
- 注意：LoremFlickr 海外服务，浏览器直连；国内部署个别图或加载慢，可在 `SAMPLE_TRAVELS[].photos` 替换自有图床 URL（已集中，易改）。备选图源测试：unsplash 200 / loremflickr 302 / picsum 302 均可达。

## 管理后台 + 用户空间限制（功能完成，已 push aca5527）
- 需求：增加管理端（用户增删改查）、限制用户图片上传空间。
- 形态：用户选「独立桌面管理后台」+「环境变量自动建首个管理员」。
- 改动：
  - `users` 表加 `is_admin TINYINT DEFAULT 0`（`init.sql` + 运行库 ALTER）；`server/.env` 写本地引导凭据（admin/admin123456，已被 gitignore），`server/.env.example` 入库。
  - `server/src/config.js` 读 `ADMIN_*` → `adminBootstrap`；两个 compose 的 backend 加 `ADMIN_*` 环境变量（默认 admin/admin123456）。
  - `server/src/middleware/auth.js`：`signToken`/`req.user` 携带 `is_admin`；`routes/auth.js` 的 `PUBLIC_FIELDS` 暴露 `is_admin`。
  - `server/src/middleware/admin.js` 新增 `requireAdmin`；`server/src/routes/admin.js` 用户 CRUD：列表(含 storage_used/limit/旅行数)、搜索、新增、编辑(昵称/密码/storageLimitGB/is_admin)、删除(禁删自己)。`index.js` 挂载 `/api/admin/users` + 启动 bootstrapAdmin。
  - `routes/upload.js`：上传前校验 `storage_used + size > storage_limit` 拒绝（403），空间限制从此真正生效。
  - 前端：`http.js` 加 `adminApi`；`stores/auth.js` 加 `isAdmin`；`Mine.vue` 仅管理员可见「用户管理」入口跳 `/admin.html`；新增 `admin.html` + `src/admin/`(Login/Users/App/router/main，hash 路由，自定义 CSS 不引新组件库)，`vite.config.js` 多入口。
- 验证：admin/admin123456 登录成功(is_admin=true)；/api/admin/users 返回用户列表含空间用量；普通用户访问 403；创建 storageLimitGB=0 用户→limitBytes=0、上传被 403 拦截；管理员(2GB)上传 200；/admin.html HTTP 200；前端 admin chunk 含"用户管理"文案。所有测试用户已清理。
- 坑：创建/编辑用户时 `storageLimitGB=0` 初版被判为"未传"回退默认 2GB，已改判 `!= null`。push 曾因 GitHub 443 临时中断，重试成功（`aca5527`）。
- 部署提醒：服务器务必在两个 compose 的 backend 环境变量设真实 `ADMIN_*`；`server/.env` 本地凭据不入库（用 `.env.example` 模板）。

## 管理端登录与客户端隔离（本次）
- 需求：用户要做 App，管理端需独立，与客户端账号体系分开。
- 实现：`server/src/routes/auth.js` 新增独立接口 `POST /api/auth/admin-login`，仅 `is_admin=1` 能登录、非管理员/不存在/密码错统一 `401` 且不签发 token；`front-end/src/admin/views/Login.vue` 改调 `admin-login`，去掉前端 is_admin 判断。客户端 `/auth/login` 不变。
- 验证：管理员 admin-login→200+token；普通用户 admin-login→401；管理员密码错→401；普通用户 /auth/login→200（客户端不受影响）；/admin.html→200。测试用户 normalx1 已清理。
- 已 commit+push (`54264c6`)。

## 禁止删除管理员账号（本次）
- 需求：管理端管理员账号不能被删除（避免后台被清空锁死）。
- 实现：`server/src/routes/admin.js` 的 `DELETE /api/admin/users/:id` 升级为"目标用户 `is_admin=1` 直接 400「管理员账号不可删除」"（原只禁删自己）；`front-end/src/admin/views/Users.vue` 对 `u.is_admin` 的删除按钮加 `:disabled` + 置灰样式 + title 提示。
- 验证：admin 登录后删 admin(id=9)→400「管理员账号不可删除」；删普通用户 wzs2→200（对照，普通用户删除正常）。
- 已 commit+push (`cf96738`)，`docker compose up -d --build backend frontend` 重建并重启，mysql 数据保留。
- 注：测试时删掉了普通用户 wzs2（非管理员，正常删除）。

## 示例清单封面使用相册首图（本次）
- 问题：示例 seed 时 `cover` 硬编码空串，相册有图但封面是渐变占位色。
- 修复：后端 `server/src/routes/travels.js` 的 `seedSampleTravels` 把 `cover` 设为 `photos[0].url`（数据层）；前端 `front-end/src/views/Lists.vue` 的 `coverStyle` 兜底：`cover` 为空时用 `t.photos?.[0]?.url`（通用，所有有相册的旅行都显示首图）。运行库 UPDATE 回填现有 is_sample=1 且 cover 空记录的封面（`JSON_UNQUOTE(JSON_EXTRACT(content,'$.photos[0].url'))`）。
- 验证：现有示例 id=21(京都)/22(杭州) cover 已填充首图；新注册用户 seed cover=photos[0] url；前端兜底生效。测试用户 cover_test_<ts> 已清理。
- 已 commit+push (`6ed5ace`)，`docker compose up -d --build backend frontend` 重建。
- git 代理(127.0.0.1:11888)再次复活，unset 后 push 成功。
