# 2026-07-30 工作日志

## 旅行 H5 多项 UI/逻辑改造（已提交 5f8f660 并 push）
- 响应式：去掉 #app / AppTabBar 的 max-width:480px 限制，改为流式宽度；相册网格改 repeat(auto-fill, minmax(96px,1fr))。
- 过去行程自动状态：store.addTravel 新增 deriveStatus(start,end)，结束<今天→done(历史手账)、开始>今天→planning、否则 ongoing；支持添加过去的行程作为日记/手账。
- 日期默认今天：Lists.vue 新建旅行的开始/结束日期默认今天，日期选择器无值时回退到今天（不再默认 2016）。
- 行程时间改 picker + 交通：TravelDetail 行程时间由手填改为 t-date-time-picker(mode=minute, 格式 YYYY-MM-DD HH:mm)；行程改为固定时间点按时间排序(dayOf 由 trip.startDate 推算)；新增到达交通方式(t-picker 单列：步行/打车/滴滴/地铁公交/公交/骑行/自驾/高铁/飞机/其他)，公共交通(地铁公交/公交)显示「线路提示」输入；时间轴在两行程间显示交通连接段(emoji+方式+线路)。
- 本地 npm run build 通过；docker compose up -d --build frontend 重建镜像并重启验证；deploy.tar.gz 重打(14:17) 供服务器更新。
- 踩坑：① t-picker 单列 columns 须为 [[...]]（数组套数组），value/confirm 的 value 为数组；② 沙箱会话间 git 死代理 127.0.0.1:11888 复发，已 `git config --global --unset http.proxy/https.proxy` 后推送成功。

## 交通方式 picker 选项为空（已提交 d111426 并 push）
- 根因：tdesign-mobile-vue(v1.15.1) 的 t-picker `columns` 每一项必须是 `{label,value}` 对象；原代码传纯字符串数组 `['步行',...]`，picker-item 用 `get(option,'label')` 取值得到 undefined → 渲染出空 `<li>`（选项在但空白）。性别 picker(genderColumns) 写法正确无此问题。
- 修复：TravelDetail.vue 的 `transportColumns` 改为 `[transportModes.map(m=>({label:m,value:m}))]`；新增 `openTransport()` 在重新打开时把已选 `af.transportMode` 预置到 `transportVal`（value 数组）。
- 已 `npm run build` 通过；重建 `travel-frontend:latest` 镜像、`docker save` 导出 `deploy/travel-images.tar`、重打 `deploy.tar.gz`(14:34)。服务器更新：传新包 → `docker load -i travel-images.tar` → 编排「更新/重新部署」或 `docker compose up -d --force-recreate frontend`（勿删栈保 mysql 数据）。

## 上传 413 + 照片标签（已提交 eccb17e 并 push）
- 413 根因：前端 nginx 反代 `client_max_body_size` 默认 1m，手机照片超限。修复：`front-end/nginx.conf` server 块加 `client_max_body_size 50m;`。
- 照片标签需求：用户想知道每张照片在哪拍的。实现：
  - 上传改为底部弹窗（点选多图 + 「拍摄地点/标签」输入），确认后逐张上传并写入 `tag`；缩略图左下角铅笔图标可事后改单张标签（弹窗输入 → store.updatePhotoTag）。
  - 照片对象形状由 `{url,size,name}` 扩展为 `{url,size,name,tag}`；`travel.js` store 新增 `updatePhotoTag(travelId,index,tag)`。
  - 数据存于 `travels.content.photos` JSON，未改表结构（仍是 content JSON 承载）。
  - 缩略图底部显示标签徽标（`.album-tag`），左上铅笔(`.album-edit`)改标签，右下 X 删除。
- `npm run build` 通过；重建 `travel-frontend:latest` 镜像、`docker save` 导出 `deploy/travel-images.tar`、重打 `deploy.tar.gz`(14:59)。服务器更新同前（load 镜像 + force-recreate frontend）。

## 15:00 后端图片压缩+相册功能增强（提交 ff28703）
- 用户：手机原图过大 + 服务器仅 6M 带宽，要求后台压缩、缩略图、查看器双指缩放/左右切换、编辑按钮可设封面+标签+拍摄地、上传/编辑下拉选已有标签或新增、列表按标签筛选。
- **后端压缩**：`server/src/routes/upload.js` 引入 `sharp`。上传后：`rotate()` 按 EXIF 转正 → `resize(1600)` 质量82 mozjpeg 大图；`resize(480)` 质量70 mozjpeg 缩略图；删原图。`server/package.json` 加 `sharp@^0.33.5` 并 `npm install` 更新 lockfile。返回 `{url, thumb, name, size}`。验证：docker run 内 sharp libvips 8.15.3 正常，4000x3000 图压缩通过。
- **前端 store**(`travel.js`)：`addPhoto` 存 `{url,thumb,size,name,tag,place}`，首图自动设封面；`updatePhotoMeta(id,idx,{tag,place})` 取代旧 `updatePhotoTag`；`setCover(id,url)`；`removePhoto` 删的是封面则回退到剩余首图缩略图。
- **TravelDetail.vue**：相册网格改显 `p.thumb`（更小 82px）；铅笔按钮→「照片设置」弹窗（设为封面 + 标签 + 拍摄地，含已有标签/地点 quick-chip 快速填入）；上传弹窗拆分标签/拍摄地两输入 + quick-chip；`t-image-viewer` 加 `:show-index :max-zoom=3 :loop`（内置 useGesture 双指缩放 + Swiper 左右切换）。
- **Lists.vue**：封面用 `t.cover`(缩略图)；新增按标签筛选 chip 行（`allTags` 去重，与状态筛选叠加）。
- 注意：照片 `tag`/`place` 均为 content JSON 字段，**未改表**，无需迁移。BASE_URL 在 compose 为空→相对路径（生产 nginx 反代 /uploads）。
- 已 push；重建 travel-backend(含 sharp)+travel-frontend 镜像、`docker save` 导出、重打 `deploy.tar.gz`(15:48, 109MB)。
