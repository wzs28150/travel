# 2026-07-28

## 应用改名 + Docker 部署（旅迹）
- **改名**：猫途迹 -> **旅迹**（用户要求不要和仿照的对象同名）。改动点：front-end/index.html `<title>`、Login/Register 品牌字与 🐱 logo(改 🧭)、Mine「关于旅迹」、server/src/index.js 启动日志、front-end/package.json name(cat-travel-h5->travel-h5)、http.js token key(cat-token->lvji-token)。
- **关键修复**：server/src/config.js 的 `baseUrl` 原用 `||`，Docker 下设 `BASE_URL=""` 会被兜底成默认地址，导致图片返回绝对 URL 走不通；改为 `??`（nullish）保留空串，使上传返回相对路径 `/uploads/xxx`，由前端 nginx 反代。
- **Docker 化**：
  - `server/Dockerfile`：node:22-alpine，npm ci --omit=dev 仅装生产依赖，COPY 源码，mkdir uploads。
  - `front-end/Dockerfile`：多阶段（build: node:22-alpine + vite build -> nginx:alpine 托管 dist）。
  - `front-end/nginx.conf`：`/` SPA 回退；`/api/` 与 `/uploads/` 反代到 `http://backend:3000`。
  - 两端各加 `.dockerignore`（排除 node_modules/.env/uploads/.workbuddy 等）。
  - 根 `docker-compose.yml`：mysql(8.0, healthcheck, init.sql 自动建表) + backend(BASE_URL=""、DB_HOST=mysql 等 env、uploads 挂卷) + frontend(8080:80)。
- **已验证**：本沙箱 Docker daemon 可用（`docker info` RUNNING）。`docker compose build` 成功产出 `travel-backend`、`travel-frontend` 镜像；`docker compose up -d` 三容器全起；经 `http://localhost:8080` 跑通 register->me->create travel->list->401 全链路。mysql 初始化(init.sql)正常，日期 `dateStrings:true` 生效为 YYYY-MM-DD。
- 注意：3000 仅容器内部暴露(`expose`)，主机经 8080 的 nginx 访问；backend 直接 3000 主机不可达属正常。
- 已提交 `c931b1a`（未 push，等待用户确认）。

## 用户 Git 工作流规则 + push 修复
- **用户规则**：以后所有改动 commit 后必须**直接 push 到远程**，不再询问确认。已写入 ~/.workbuddy/MEMORY.md 与项目 MEMORY.md。
- **push 失败排查**：本次 `git push` 报 "via 127.0.0.1 port 443" 连不上 GitHub。根因是 git 全局代理 `http.proxy/https.proxy=http://127.0.0.1:11888` 指向已死本地端口（沙箱注入，但该进程当前未运行）。直连 GitHub 正常（curl https://github.com=200）。已 `git config --global --unset http.proxy https.proxy` 清除，普通 `git push origin main` 恢复正常；并用 `-c http.proxy=` 临时绕过先把 `c931b1a` 推上去了。
- 该代理可能随沙箱会话重新注入，下次 push 失败优先排查此项。

## 生成离线部署包（deploy/）
- `docker save` 导出 `travel-backend:latest` + `travel-frontend:latest` 为 `deploy/travel-images.tar`（81MB，含 node/nginx 基础层，已 load 回校验可正常载入）。
- `deploy/docker-compose.yml`：部署版，用 `image:` 直接 load 后的镜像（不依赖源码）；`deploy/docker-compose.build.yml` 备用（本地 build 源码）；`deploy/init.sql` 复制一份供 mysql 挂载初始化；`deploy/README.md` 部署说明。
- `.gitignore` 增加 `deploy/*.tar`（大二进制不入库，单独传）。已提交 `c3dabb0` 并 push。
- 镜像位置：本地 Docker daemon（非散文件）；部署靠 `docker save` 导 tar 或推 registry。mysql:8.0 由 compose 自动拉取（离线需先 pull）。
- 用户要「一个 tar 传到 Linux 服务器」：将 deploy/ 下全部文件 + travel-images.tar 合并打包为 `travel-deploy.tar.gz`（81MB，含 travel-deploy/ 目录）。已 gitignore，不入库。Linux 服务器: tar -xzf → cd travel-deploy → docker load -i travel-images.tar → docker compose up -d。镜像为 amd64 架构。

## 端口变更：后端 8089 / 前端 8090
- 用户要求后端端口 3000->**8089**，前端对外端口 8080->**8090**。
- 改动点：server/src/config.js（默认 PORT 与 baseUrl）、server/Dockerfile(EXPOSE)、根 docker-compose.yml、deploy/ 与 travel-deploy/ 下的 docker-compose.yml + docker-compose.build.yml（PORT env、expose、前端 ports）、front-end/nginx.conf（两处 proxy_pass 改 backend:**8089**）、front-end/vite.config.js(dev proxy target)、两份 README 端口说明。
- 已提交 **2bc1665** 并 push（含 .gitignore 既有改动）。travel-deploy/ 被 gitignore，其内副本也同步改了但不同步入库。
- 注意：重新构建镜像/打包 tar 才会反映新端口；旧的 travel-deploy.tar.gz 仍是旧端口，需重新导出。

## 合并部署目录：统一为唯一 deploy/
- 用户嫌 `deploy/` 与 `travel-deploy/` 两个目录易混淆，要求只留一个。已合并为**唯一 `deploy/`**，删除 `travel-deploy/`（含 travel-deploy.tar.gz）。
- 流程：后台 `docker compose build` 重建新端口镜像 -> `docker save` 导出为 `deploy/travel-images.tar` -> `tar -czf deploy.tar.gz deploy/` 生成唯一传输包。
- 更新 `.gitignore`：删 `travel-deploy/`、`travel-deploy.tar.gz` 两条；保留 `deploy/*.tar` 并新增 `deploy.tar.gz`（均不入库，大二进制单独传）。
- `deploy/README.md` 顶部加醒目提示：本项目只有这一个部署目录，原 travel-deploy 已合并。
- 已提交 **7f02a2d** 并 push。现 deploy/ 即唯一部署源（git 跟踪配置，本地含 travel-images.tar；deploy.tar.gz 是上传用的压缩包）。
- 服务器部署：传 `deploy.tar.gz` -> `tar -xzf deploy.tar.gz` -> `cd deploy` -> `docker load -i travel-images.tar` -> `docker compose up -d` -> 访问 :8090。

## 部署架构改为双环境（服务器不用 Docker 全栈）
- 用户服务器已自带 MySQL（3306 被占用），决定**生产服务器不用 Docker 全栈**（mysql 容器会与系统 MySQL 抢 3306）；**本地测试仍用 Docker**（含 mysql 容器）。
- 重写 `deploy/README.md` 为双方案：方式 A=本地/测试 Docker 全栈（根 `docker-compose.yml`，含 mysql 容器，访问 localhost:8090）；方式 B=生产服务器原生部署（用系统 MySQL，后端 `node src/index.js` + `.env` 连 `127.0.0.1:3306`，前端 `vite build` 的 `dist/` 由 nginx 反代到 `127.0.0.1:8089`，对外 8090；可借宝塔 Node 项目+网站反代）。
- 新增 `server/.env.example`（系统 MySQL 连接参数 + JWT_SECRET 占位）。后端 `import 'dotenv/config'` 自动加载。
- 根 `docker-compose.yml` 顶部加注释标明「本地测试用，含 mysql 容器，生产服务器勿用」。
- 已提交 **064cd64** 并 push（3 文件：server/.env.example、deploy/README.md、docker-compose.yml）。

## 服务器改用 Docker 全栈 + 注册 502 排查
- 用户推翻「服务器不用 Docker」，**生产服务器也用 Docker 全栈**（deploy/docker-compose.yml 含 mysql 容器）。三容器均 Up，前端页面可访问，但注册 `/api/auth/register` 报 502。
- 代码层已核对：前端 `nginx.conf` 反代 `http://backend:8089`（无尾斜杠，路径正确）；后端 `app.listen(config.port)` 默认 0.0.0.0:8089。配置无误 → 502 是 nginx 连不上 backend upstream。
- 已给服务器诊断命令：`docker ps`（看 mysql/backend 是否 Restarting/healthy）、`docker logs travel_backend/travel_mysql`、`docker exec travel_frontend wget -qO- http://backend:8089/api/health`、`docker exec travel_frontend grep proxy_pass /etc/nginx/`。
- 最大嫌疑：服务器系统 MySQL 占 3306 与 docker mysql 容器 `3306:3306` 冲突 → mysql 不健康 → backend 未启动 → 502。待用户回报日志确认。（修复：停系统 MySQL 或改 mysql 端口映射 3307:3306，DB_PORT 仍 3306）

## 前端 UI 升级（背景/卡片/动画 + Mine 宽度对齐）
- 用户反馈：1)「我的」页存储空间卡片与下方列表宽度不一致；2) 整体纯橙背景太单一太简单，要高级感+动画。
- 根因（宽度不一致）：`.page-scroll` 有 `padding:12px`，`.storage-card`(.card 类)撑满内容区；而 `t-cell-group theme="card"` 在 tdesign 移动端默认带左右外边距，导致列表比存储卡片窄一截。
- 修复：Mine.vue 给 `.storage-card` 与 `:deep(.t-cell-group)` 统一设 `margin: 0 12px 12px`，两者对齐。
- 全局视觉升级（`front-end/src/styles/global.css`）：
  - `--page-bg` 由纯浅灰改为**柔和暖橙→中性浅灰渐变** `linear-gradient(180deg,#fff6f0,#f6f7f9,#f4f5f8)`；`--brand-gradient` 更通透。
  - `.card` 圆角 16px、加 1px 淡描边、阴影升级 `--shadow-card`（双层柔和）、加过渡。
  - 新增 `@keyframes fadeInUp`，`.page-scroll > *` 加**错峰入场动画**（nth-child 递增 delay），所有卡片/列表进入即上浮淡入；尊重 `prefers-reduced-motion`。
  - `.app-header`/`.mine-header` 加 `::after` 径向高光提升质感。
  - 页面切换过渡：`App.vue` 的 `<router-view>` 外套 `<transition name="page" mode="out-in">`，tab 切换淡入上移。
- Mine.vue 精修：头像白边描边、mini-stats 玻璃态(`backdrop-filter`+半透明白+分隔线)、存储卡片描边、进度条配色更雅。
- 源码已提交 **6dfd8ed** 并 push（3 文件：global.css, App.vue, Mine.vue）；`.gitignore` 加 `deploy/.tmp-*` 忽略 docker save 临时文件。
- 后台 `docker compose build` 重建镜像 + `docker save` + 重打 `deploy.tar.gz`（任务 #21 已完成，文件 14:56）。改前端后须重传 `deploy.tar.gz` 到服务器 `docker load`+`docker compose up -d` 才生效。
- 已本地验证新前端镜像内 `proxy_pass http://backend:8089`（不会再 502）。告知用户在宝塔编排里重新部署步骤：传新 deploy.tar.gz→解压→load 新镜像→编排「更新/重新部署」或 `docker compose up -d --force-recreate frontend backend`（勿删栈，保 mysql_data）。
- 用户要在本地看页面：本沙箱 `docker compose up -d` 全栈成功（travel_mysql healthy / travel_backend 8089 Up / travel_frontend 8090 Up，后端日志「MySQL 连接成功」），前端 GET / -> HTTP_200。本地预览地址 **http://localhost:8090**（注册账号登录看完整页面）。
- 目的地选择器重构（用户要求「选全国省市区」而非部分城市）：移除按 REGIONS 分组的 chip 多选，改用 tdesign `t-cascader` 三级联动（全国 31 省/市/区）。数据来源 `npm pack china-division` 解包取 `dist/pca-code.json`（[{code,name,children}]），落地为 `front-end/src/data/china-pca.json`（137KB，提交入库，不引 npm 依赖避免 docker 构建联网）。`t-cascader` 是单选值(value=叶子 code)，用 `:keys="{label:'name',value:'code',children:'children'}"` + `:check-strictly="true"` + `@change(value, selectedOptions)` 拿整条路径；`Lists.vue` 由此派生 `regionText`(省/市/区) 与 `cityName`（足迹墙兼容）。`store.addTravel` 改为优先 `data.cityName`→`data.destination`。已提交 **16a0361** 并 push。本地 `docker compose up -d --build frontend` 重建验证构建通过，localhost:8090 可预览；`deploy.tar.gz` 重打（16:09）。
