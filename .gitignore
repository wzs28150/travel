# 依赖
node_modules/

# 构建产物
dist/

# 环境变量 / 密钥
.env

# 本地项目状态（WorkBuddy 运行时数据，非源码）
.workbuddy/

# 操作系统 / 编辑器
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# 部署导出镜像（大二进制，单独传输，不入库）
deploy/*.tar
# 合并打包的部署产物（含镜像 tar + 配置，单独传输服务器，不入库）
travel-deploy/
travel-deploy.tar.gz
