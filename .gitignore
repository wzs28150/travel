# 依赖
node_modules/

# 构建产物
dist/

# 环境变量 / 密钥
.env

# 本地项目状态（WorkBuddy 运行时数据）—— 整个目录纳入版本控制，便于多机同步 AI 记忆与配置

# 操作系统 / 编辑器
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# 部署目录 deploy/ 内的镜像与压缩包（大二进制，单独传输服务器，不入库）
deploy/*.tar
deploy.tar.gz
deploy/.tmp-*
